## How to run

1. Run `npm install`
2. Make sure MongoDB is running on `mongodb://localhost:27017`
3. Run any seed script if you have one (e.g. `node tasks/seed.js`)
4. Start the server with `npm start`
5. Open `http://localhost:3000` in your browser
